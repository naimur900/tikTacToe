// import React from "react";
// import Box from "./Box";

// const Board = ({ board, handleClick }) => {
//   return (
//     <>
//       {board.map((value, idx) => {
//         return <Box value={value} handleClick={handleClick} idx={idx} />;
//       })}
//     </>
//   );
// };

// export default Board;
